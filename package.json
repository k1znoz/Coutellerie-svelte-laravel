{
    "name": "coutellerie-svelte-laravel",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "build": "cd apps/coutellerie-svelte && npm ci && npx svelte-kit sync && npm run build",
        "install-php": "cd services/coutellerie-laravel && composer install --no-dev --optimize-autoloader --no-interaction",
        "start": "cd services/coutellerie-laravel && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=${PORT:-8000}"
    },
    "engines": {
        "node": "18.x"
    }
}