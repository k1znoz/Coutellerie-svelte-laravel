{
    "name": "coutellerie-svelte-laravel",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "build": "cd apps/coutellerie-svelte && npm ci && npx svelte-kit sync && npm run build",
        "start": "cd services/coutellerie-laravel && php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
    },
    "engines": {
        "node": "18.x"
    }
}