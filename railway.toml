[build]
builder = "nixpacks"

[deploy]
startCommand = "cd services/coutellerie-laravel && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
restartPolicyType = "always"

[variables]
NIXPACKS_NO_DOCKERFILE = "1"