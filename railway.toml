[build]
builder = "nixpacks"

[deploy]
startCommand = "cd services/coutellerie-laravel && php artisan serve --host=0.0.0.0 --port=$PORT"
restartPolicyType = "always"

[variables]
NODE_VERSION = "18"
PHP_VERSION = "8.2"